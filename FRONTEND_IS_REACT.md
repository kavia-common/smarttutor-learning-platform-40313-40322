Frontend is React (Vite + TypeScript)

- Path: smarttutor-learning-platform-40313-40322/react_frontend
- Do not run Flutter analyzers or treat this workspace as a Flutter project.
- Use npm install / npm run build (or npm run dev) for the web app.

For CI:
- See run_ci.sh which runs backend tests and builds the React frontend only.
