# API endpoints (optional for mock mode)
API_BASE_URL=
WS_BASE_URL=

# Use mocks to run fully offline without a backend
FEATURE_USE_MOCKS=true

# Stripe publishable key (optional, for future client flows)
STRIPE_PUBLISHABLE_KEY=
